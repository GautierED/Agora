import React, { useEffect } from "react";

const Inventory = () => {

    useEffect(() => {
        let listItems = [];
        let message = 'hello';

        const init = async () => {

        };    
        init();
    }, []);    


    return (
        <div>
            {message}
        </div>
    );
};

export default Inventory;